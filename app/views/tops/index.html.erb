<div class="container mx-auto p-4">
  <!-- ヘッダー部分 -->
  <div class="flex justify-between items-center my-6">
    <h1 class="text-4xl">この中で、一番思い出感があるフォントは？？</h1>
    <div id="flash" class="text-red-500"></div>
  </div>

  <!-- フォント表示部分 -->
  <div class="container mx-auto p-4">
    <div class="grid grid-cols-1 gap-8">
      <% @fonts.each do |font| %>
        <div class="p-6 shadow-lg rounded-lg bg-white">
          <!-- フォント表示部分 -->
          <p class="<%= font.class_name %> text-5xl mb-4">
            <%= font.name %> <br>
            思い出 おもいで omoide memory ....
          </p>

            <div class="flex justify-end items-center">
            <div id="likes_count_<%= font.id %>">
            <p class="text-lg font-bold mr-4">いいね数: <%= font.likes.count %></p> <!-- `likes_count`から`likes.count`に変更 -->
            </div>

            <div>
              <%= form_with url: toggle_like_font_path(font), method: :post, remote: true do %> <!-- `remote: true`を追加 -->
                <button type="submit" class="btn px-4 py-2">いいね</button>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
